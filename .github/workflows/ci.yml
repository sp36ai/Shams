name: CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  app-quality:
    name: App Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Dependency audit (production)
        # Fail on high/critical vulnerabilities in production dependencies.
        # Dev-only tooling advisories (e.g. RN CLI) are excluded — they don't
        # ship and are gated on upstream RN, not this repo.
        run: npm audit --omit=dev --audit-level=high

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test -- --runInBand

      - name: Dead code guard (orphans)
        run: npm run check:orphans

  functions-quality:
    name: Functions Quality
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: functions
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: functions/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Dependency audit (production)
        # Fail on high/critical vulnerabilities in Cloud Functions production
        # dependencies (firebase-admin/google-cloud transitive chain). Dev-only
        # test-tooling advisories are excluded.
        run: npm audit --omit=dev --audit-level=high

      - name: Engine sync guard
        # functions/src/engine is a generated mirror of src/astrology (produced
        # by sync-engine). Fail if the committed mirror has drifted from the
        # source of truth — a drift means the deployed server engine would
        # differ from the reviewed source until the next deploy regenerates it.
        run: |
          npm run sync-engine
          if ! git diff --quiet -- src/engine; then
            echo "::error::functions/src/engine is out of sync with src/astrology. Run 'npm --prefix functions run sync-engine' and commit the result."
            git --no-pager diff --stat -- src/engine
            exit 1
          fi

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Unit tests
        run: npm test -- --run

  e2e:
    name: E2E Tests (Maestro)
    runs-on: ubuntu-latest
    needs: app-quality
    timeout-minutes: 60
    permissions:
      contents: read
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # Use real google-services.json from secret if available,
      # otherwise fall back to the CI mock (structure-valid, placeholder creds).
      # The mock allows Gradle to compile; Firebase calls will fail at runtime
      # which is acceptable — E2E flows test UI logic, not live Firebase.
      - name: Write google-services.json
        run: |
          if [ -n "${{ secrets.GOOGLE_SERVICES_JSON }}" ]; then
            echo "${{ secrets.GOOGLE_SERVICES_JSON }}" | base64 -d > android/app/google-services.json
            echo "Using real google-services.json from secret"
          else
            cp android/app/google-services.json.ci android/app/google-services.json
            echo "Using CI mock google-services.json (no secret configured)"
          fi

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('android/**/*.gradle*', 'android/gradle.properties') }}
          restore-keys: gradle-

      # debug.keystore is gitignored (not a secret, but not committed either);
      # generate it fresh with the exact alias/passwords signingConfigs.debug expects.
      - name: Generate debug keystore
        run: |
          keytool -genkeypair -v -keystore android/app/debug.keystore \
            -storepass android -keypass android -alias androiddebugkey \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US"

      # -Pe2eBundleJs embeds the JS bundle so the app runs without Metro
      - name: Build debug APK (embedded JS bundle)
        run: cd android && chmod +x gradlew && ./gradlew assembleDebug -Pe2eBundleJs=true --no-daemon -q

      - name: Enable KVM (emulator acceleration)
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Setup Android emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 31
          arch: x86_64
          target: google_apis
          profile: pixel_6
          avd-name: shams_test
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          # NOTE: the runner executes each script line in a separate shell, so
          # env exports do not persist between lines — keep commands self-contained.
          script: |
            adb install -r android/app/build/outputs/apk/debug/app-debug.apk
            adb emu geo fix 67.0011 24.8607
            curl -Ls "https://get.maestro.mobile.dev" | MAESTRO_VERSION=1.39.0 bash
            "$HOME/.maestro/bin/maestro" test --format junit --output maestro-results.xml .maestro/ci/ || true

      - name: Upload Maestro results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: maestro-results
          path: |
            maestro-results.xml
            ~/.maestro/tests/

      - name: Publish test report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Maestro E2E Results
          path: maestro-results.xml
          reporter: java-junit
          fail-on-error: false

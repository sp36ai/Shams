# CLAUDE.md

Notes for Claude Code sessions working in this repo.

## Project

Shams al-Asrār — a React Native (Firebase-backed) horary astrology app.
App code lives in `src/`; Cloud Functions in `functions/`. See `README.md`
and `docs/` for the design system and architecture docs.

## Deferred follow-ups

- **Full dead-wood cleanup** — a pass to remove unused code, stale
  components/assets, and outdated status/report markdown files at the repo
  root (there are many one-off `*_STATUS.md`, `*_AUDIT.md`,
  `*_SUMMARY.md`, etc. from earlier work). **Do not start this
  unprompted.** Wait until the user says the app has reached production
  level, then ask before beginning — it's a broad, higher-risk change and
  should be scoped and confirmed with them first.

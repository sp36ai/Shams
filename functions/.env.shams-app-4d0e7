# Non-secret Firebase Functions params for project shams-app-4d0e7.
# Actual secrets (Razorpay, Google Play, Anthropic) are bound via Secret
# Manager through defineSecret() in config.ts — never put them here.

RATE_LIMIT_PER_MINUTE=10

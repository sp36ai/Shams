# Firebase Functions params for project shams-app-4d0e7
# Loaded automatically during `firebase deploy` for this project.
# DO NOT commit secrets here — secrets go in Secret Manager.
RATE_LIMIT_PER_MINUTE=10
